
function normalizeProfileMap(profile) {
    const result = {};

    // process each key in the profile to build the result
    // first line 153 to 168 are kept but wrapped in the clone function
    // ... (other processing logic)

    return clone(result); // Updated line 167
}
