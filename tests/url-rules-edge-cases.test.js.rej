--- url-rules-edge-cases.test.js
+++ url-rules-edge-cases.test.js
@@ -134,7 +134,7 @@
       urlPattern: "*://example.com/*"
     }
   };
-  assert.deepEqual(root.ExtensityUrlRules.resolveChanges(rules, "https://example.com/"), expected);
+  assert.deepEqual(normalize(root.ExtensityUrlRules.resolveChanges(rules, "https://example.com/")), expected);
 });

 test("resolveChanges: applies enable and disable for different extensions independently", () => {
