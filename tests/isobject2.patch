--- tests/storage-schema.test.js
+++ tests/storage-schema.test.js
@@ -83,7 +83,7 @@
   const first = root.ExtensityStorage.getSyncDefaults();
   const second = root.ExtensityStorage.getSyncDefaults();
   first.sortMode = "alpha";
-  assert.strictEqual(second.sortMode, "recent", "getSyncDefaults must return independent copies");
+  assert.strictEqual(normalize(second).sortMode, "recent", "getSyncDefaults must return independent copies");
 });

 test("getLocalDefaults returns a fresh clone on each call", () => {
